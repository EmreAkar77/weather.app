Installation
Clone the project:
git clone ...
Install dependencies:
npm install
Create .env file:
Copy the .env.example file and rename it to .env
Add your API key:
API_KEY=your_api_key_here
Run the project:
node weather.js
